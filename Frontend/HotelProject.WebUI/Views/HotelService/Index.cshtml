@model List<ResultHotelServiceDTO>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

<div class="col-lg-12">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Personel Listesi</h4>
            <div class="table-responsive">
                <table class="table table-bordered verticle-middle">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Ikon</th>
                            <th scope="col">Başlık</th>
                            <th scope="col">Açıklama</th>
                            <th scope="col">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.HotelServiceID</td>
                                <td>@item.HotelServiceIcon</td>
                                <td>@item.HotelServiceTitle</td>
                                <td>@item.HotelServiceDescription</td>
                                <td>
                                    <span class="mr-3">
                                        <a asp-action="UpdateHotelService" asp-route-id="@item.HotelServiceID" data-toggle="tooltip" data-placement="top" title="" data-original-title="Güncelle"><i class="fa fa-pencil color-muted m-r-5"></i> </a>
                                    </span>
                                    <span class="mr-3">
                                        <a asp-action="DeleteHotelService" asp-route-id="@item.HotelServiceID" data-toggle="tooltip" data-placement="top" title="" data-original-title="Sil"><i class="fa fa-close color-danger"></i></a>
                                    </span>
                                    <span class="mr-3">
                                        <a href="#" data-toggle="tooltip" data-placement="top" title="" data-original-title="Detaylar"><i class="fa fa-file  color-danger"></i></a>
                                    </span>
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
                <a asp-action="AddHotelService" class="btn mb-1 btn-rounded btn-outline-info">Yeni Hizmet Ekle</a>
            </div>
        </div>
    </div>
</div>

